[package]
name = "detector-go"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
scanner-core = { path = "../scanner-core" }
anyhow = { workspace = true }

[lib]
name = "detector_go"
path = "src/lib.rs"

